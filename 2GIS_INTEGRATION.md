# Интеграция 2ГИС Maps API

## Обзор

Реализована интеграция карты 2ГИС для отображения инфраструктуры вокруг жилых комплексов на странице детального просмотра.

## Что было реализовано

### 1. Сервис для работы с 2ГИС API
**Файл:** `frontend/src/services/Infrastructure2GISService.js`

- Поиск объектов инфраструктуры в радиусе 1000 метров от ЖК
- Категории поиска:
  - 🎓 **Образование**: школы, детские сады
  - 🛒 **Покупки и развлечения**: супермаркеты, магазины, торговые центры
  - 🌳 **Парки и отдых**: парки, скверы
  - 🏥 **Здоровье**: больницы, поликлиники, аптеки
- Автоматический расчет расстояния от ЖК до объекта
- API ключ: `b4bef9b9-dad2-40a6-af72-5ad0f37227f4`

### 2. Компонент карты 2ГИС
**Файл:** `frontend/src/components/Complex/Map2GIS.jsx`

- Интерактивная карта с использованием 2GIS Maps API 2.0
- Отображение маркера ЖК (иконка здания)
- Цветные маркеры для разных категорий инфраструктуры
- Всплывающие окна (popup) с информацией о объекте
- Легенда карты с категориями
- Адаптивный дизайн для мобильных устройств

### 3. Обновленный компонент инфраструктуры
**Файл:** `frontend/src/components/Complex/ComplexInfrastructure.jsx`

- Автоматическая загрузка данных при открытии страницы
- Отображение карты сверху
- Список найденных объектов снизу, сгруппированных по категориям
- Состояния загрузки и ошибок

### 4. Стили
**Файлы:**
- `frontend/src/styles/Map2GIS.module.scss` - стили карты
- `frontend/src/styles/ComplexDetail.module.scss` - обновлены стили секции инфраструктуры

## Как это работает

1. **При открытии страницы ЖК:**
   - Компонент `ComplexInfrastructure` получает `latitude`, `longitude` и `complexName` из данных комплекса
   - Вызывается сервис `Infrastructure2GISService` для поиска объектов в радиусе 1 км
   - 2ГИС API возвращает данные о найденных объектах

2. **Отображение на карте:**
   - Инициализируется карта с центром на координатах ЖК
   - Добавляется маркер ЖК (иконка здания)
   - Добавляются цветные маркеры для каждого найденного объекта инфраструктуры
   - При клике на маркер показывается информация об объекте

3. **Отображение списком:**
   - Объекты группируются по категориям
   - Показывается название объекта и расстояние до него
   - Топ-5 ближайших объектов в каждой категории

## Требования к данным

Для корректной работы интеграции в таблице `residential_complex` должны быть заполнены:
- `latitude` - широта (например: 43.238949)
- `longitude` - долгота (например: 76.889709)
- `name` - название комплекса

## Настройка

### API ключ 2ГИС
API ключ хранится в файле `Infrastructure2GISService.js`:
```javascript
const API_KEY = 'b4bef9b9-dad2-40a6-af72-5ad0f37227f4';
```

### Радиус поиска
По умолчанию установлен радиус 1000 метров (1 км). Можно изменить в файле `ComplexInfrastructure.jsx`:
```javascript
const infrastructureData = await infrastructure2GISService.getInfrastructureByCategories(
    parseFloat(latitude),
    parseFloat(longitude),
    1000  // <-- изменить здесь
);
```

### Категории поиска
Категории настроены в `Infrastructure2GISService.js` в объекте `CATEGORY_QUERIES`. Можно добавить новые категории или изменить поисковые запросы.

## Тестирование

1. **Запустите бэкенд:**
   ```bash
   cd Backend
   docker-compose up
   ```

2. **Запустите фронтенд:**
   ```bash
   cd frontend
   npm start
   ```

3. **Убедитесь, что в БД есть комплекс с координатами:**
   - Откройте страницу любого ЖК (например: `/complex/1`)
   - Должна отобразиться карта с маркерами инфраструктуры
   - Под картой должен быть список найденных объектов

4. **Проверка координат:**
   Если координаты не заданы, можно использовать:
   - **Алматы центр:** lat: 43.238949, lon: 76.889709
   - **Астана центр:** lat: 51.128422, lon: 71.430564

## Возможные проблемы

### Карта не загружается
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что скрипт 2ГИС загружается (проверьте Network в DevTools)
- Проверьте, что у комплекса есть координаты

### Не находятся объекты
- Убедитесь, что координаты корректны
- Проверьте API ключ 2ГИС
- Возможно, в указанном радиусе нет объектов (попробуйте увеличить радиус)

### CORS ошибки
- 2ГИС API должен работать без проблем с CORS
- Если есть проблемы, проверьте настройки прокси в package.json

## Дальнейшие улучшения

1. **Кэширование результатов**
   - Сохранять результаты поиска в localStorage
   - Обновлять данные раз в сутки

2. **Фильтры**
   - Добавить возможность фильтровать категории
   - Показывать/скрывать маркеры по категориям

3. **Детальная информация**
   - Добавить рейтинги из 2ГИС
   - Показывать часы работы
   - Добавить фотографии объектов

4. **Маршруты**
   - Интегрировать построение маршрута от ЖК до объекта
   - Показывать время в пути пешком/на транспорте

## Документация 2ГИС API

- **Maps API:** https://api.2gis.ru/doc/maps/en/
- **Places API:** https://docs.2gis.com/en/api/search/places/overview
- **MapGL JS API:** https://docs.2gis.com/en/mapgl/overview/features

## Структура файлов

```
frontend/
├── src/
│   ├── components/
│   │   └── Complex/
│   │       ├── ComplexInfrastructure.jsx  (обновлен)
│   │       └── Map2GIS.jsx                (новый)
│   ├── services/
│   │   └── Infrastructure2GISService.js   (новый)
│   ├── styles/
│   │   ├── Map2GIS.module.scss           (новый)
│   │   └── ComplexDetail.module.scss     (обновлен)
│   └── pages/
│       └── Complex/
│           └── ComplexDetailPage.jsx      (обновлен)
```

## Контакты и поддержка

Если возникнут вопросы или проблемы, проверьте:
1. Документацию 2ГИС API
2. Логи в консоли браузера
3. Network tab в DevTools для проверки запросов к API
